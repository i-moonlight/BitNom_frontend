import { Card, Typography } from '@mui/material';
import React from 'react';

export default function MediaUploadPopover({
    uploadId,
    uploadAnchorEl,
    isUploadSettingsOpen,
    handleUploadClose,
}) {
    return (
        <Card>
            <Typography></Typography>
        </Card>
    );
}
